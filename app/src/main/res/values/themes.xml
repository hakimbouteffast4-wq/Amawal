<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Amawal" parent="Theme.Material3.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/teal_primary</item>
        <item name="colorPrimaryDark">@color/teal_dark</item>
        <item name="colorAccent">@color/teal_primary</item>
        <item name="android:windowBackground">@color/background_teal</item>
        <item name="android:statusBarColor">@color/teal_primary</item>
    </style>

    <style name="Theme.Amawal" parent="Base.Theme.Amawal" />
</resources>